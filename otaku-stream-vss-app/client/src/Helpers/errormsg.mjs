const fallback = "Unknown Server Error - please try again later";
const connection = "Could Not Connect To Server - Try Again Later"
const wrongFormatEmail = "Email Is In The Wrong Format";
const passwordDontMatch = "Password Do Not Match - This Is Case Sensitive!"
const passwordWrongLength = "Length Of The Password Must Be at least 7 Characters"

const ErrorMsg = {
    fallback,
    connection,
    wrongFormatEmail,
    passwordDontMatch,
    passwordWrongLength,
};

export default ErrorMsg;